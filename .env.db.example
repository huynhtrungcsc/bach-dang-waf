# ============================================================
#  Bach Dang WAF — Database environment
#  Copy to .env.db and set real values before deploying.
#
#  Used by:
#    docker compose -f docker-compose.db.yml up -d
#    docker compose up -d database
# ============================================================

# PostgreSQL connection
WAF_DB_NAME=bach_dang_waf
WAF_DB_USER=waf_user
WAF_DB_PASS=change_before_deploy
WAF_DB_PORT=5432
